# dependencies
node_modules
.pnpm-store

# build artifacts
dist
build
.vite
*.tsbuildinfo

# env
.env
.env.local
.env.*.local

# uploads (runtime data)
uploads
data
infra/compose/data

# Prisma-generated clients (output inside packages/db-*/src/_prisma so a
# plain ESM relative import resolves under Node's strict resolver).
packages/*/src/_prisma/

# logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# editor / OS
.DS_Store
.idea
.vscode/*
!.vscode/extensions.json

# coverage
coverage
.nyc_output

# Claude Code session state (settings + isolation worktrees) — local only
.claude/
